# **张龙涛 - Java后端开发**                                                                                                                                             

**男 | 24 岁 | 2026届硕士研究生 | 17822417902 | zlt_cs2023@163.com**  

## 教育经历

**天津理工大学              计算机技术（专业排名前10%）  	        全日制硕士                         2023.09 – 2026.06**

**安徽医科大学              电子信息（专业排名前20%）  	           全日制本科                          2019.09 – 2023.06**

## 实习经历

**联信智评数字科技有限公司  -  数据同步工具项目  -  Java 开发实习生                                   2025.06 – 2025.09** 

**项目背景：**为实现核心产品自主化和客户定制化需求，部门计划开发高效安全的数据同步工具，解决服务端到客户端数据库的跨平台、高并发同步难题。**目前项目已落地，为多家理财机构提供稳定服务。**

**个人职责：**主要负责数据同步工具中**客户端**的开发，聚焦 “异常容错”、“高并发有序性”、“多环境适配” 三大关键问题，保障跨数据库、多场景下的数据同步稳定性与效率。

**核心成果与技术实现：**

- **本地任务状态管理方案：**针对服务端连接异常导致的任务状态丢失问题，设计基于 SQLite 的本地记录表方案，实时存储任务 ID、执行状态、进度等数据。通过状态校验机制避免重复执行，减少因连接中断产生的无效操作，支撑断网后重连任务状态自动同步功能。

- **高并发有序同步架构：**面对并发任务的乱序问题，设计 “线程池 + 优先级队列” 的调度模型，按任务ID与业务优先级排序执行，可支撑理财机构每日海量数据的高效同步。

- **多数据库兼容层设计：**封装统一数据访问接口，适配 MySQL、Oracle、PostgreSQL 的语法差异（如日期函数、分页语法），通过动态 SQL 生成与数据库类型适配策略，实现跨库同步零代码改动，兼容主流数据库环境，减少 数据库适配开发量。

- **多模式高可用部署：**开发单主、主备、主主三种运行模式配置功能，支持故障自动检测与快速切换（如主节点宕机后备节点自动接管），提升客户端容错能力，保障数据同步的连续性，满足不同规模理财机构的部署需求。

  

**恒华数元科技（天津）有限公司  -  微电网控制平台项目  -  Java 开发实习生                         2024.06 – 2024.09**

**项目背景：**针对公司现有系统体量庞大、模块冗余的问题，部门计划从现有系统中抽取核心业务，开发轻量化微应用，打造模块化、可快速迭代的微电网控制平台，支撑微电网实时运营与调控需求。

**个人职责：**主要负责**数据字典**与**策略执行**模块的设计、开发与优化，解决系统响应慢、策略执行不及时的关键问题。

**核心成果与技术实现：** 

- **设计实现多级字典缓存模块：**针对接口逐条查询字典数据导致的响应缓慢问题，设计 “本地缓存 + Redis” 二级缓存方案。通过自定义注解（指定字段赋值路径）结合 AOP 的 AfterReturning 通知，自动为接口返回值填充字典名称（如 type = 01 自动补充 typeName = 运行），将多轮数据库查询优化为缓存访问，显著提升接口响应速度，减少数据库访问压力。
- **开发策略执行模块：**基于 Spring 事件驱动机制，拆分策略执行逻辑：非实时策略通过定时任务调度执行；实时策略监听第三方数据入库事务成功事件，触发事件发布与监听流程，自动生成微电网调控指令，实现 “数据入库→策略触发→指令生成” 的秒级响应，满足业务实时性需求。


## 项目经历 

**IntelliJ IDEA 插件 - SQL 自动生成与 MyBatis 代码转换工具                                               2025.05 –2025.06**

**项目背景：** 针对SQL编写耗时、MyBatis 配置繁琐，且单纯 AI 工具需手动添加表结构的痛点，开发轻量级嵌入式 IDEA 插件，无需切换工具即可实现 "数据库连接 → 自动获表结构 → AI 生成 SQL → MyBatis 转换" 全流程，提升开发效率。

**项目职责：** 独立完成插件从需求分析、架构设计、功能开发到插件商店发布的全流程。

**技术栈：** IntelliJ Platform、Swing、LangChain4j、DashScope 大模型、PropertiesComponent。

**项目亮点：**

- **嵌入式架构与流畅交互：** 基于 IntelliJ Platform 构建 ToolWindow，用 Swing 实现"数据库连接 / 生成 SQL / SQL 转换 / API 设置" 多标签界面，通过事件监听实现数据库连接成功后自动加载表结构，减少开发者手动操作步骤，界面交互符合 IDE 使用习惯，无明显卡顿。

- **AI 生成 SQL 精准度保障：** 采用 LangChain4j 框架集成 DashScope 大模型，通过 @Tool 注解实现智能工具调用，自动解析数据库表结构（字段名、类型、约束）动态生成 Prompt，确保生成 SQL 严格匹配真实表结构与字段约束，降低人工修改 SQL 的频率。

- **全链路提效与稳定性保障：** 支持 SQL 到 MyBatis XML、Lambda 代码的自动转换，减少手写映射配置的重复工作量；采用 IDEA 后台任务（Task.Backgroundable）实现大模型 API 异步调用 + 进度指示器，避免 UI 卡顿；通过 PropertiesComponent 持久化 API 配置，插件重启后自动加载配置，优化用户体验。

- **企业级架构设计：** 采用单例模式 + 双重检查锁定管理数据库连接池和 AI 服务，确保线程安全与资源复用；实现配置变更后的自动重新初始化机制，支持多轮对话记忆功能，提升 AI 交互体验。

## 专业技能

- **语言与基础：**Java 基础扎实，熟悉集合框架、并发编程、JVM 内存模型与垃圾回收机制，具备良好的编码规范。
- **开发框架：**熟练使用 Spring Boot、Spring MVC、Mybatis，理解 IoC/AOP、自动配置与事务机制。  
- **数据库与持久层：**熟练使用 MySQL，理解索引、事务、锁机制，具备 SQL 调优与慢查询优化经验。 
- **缓存与中间件：**熟悉 Redis，具备多级缓存、分布式锁实践；了解 RabbitMQ 消息机制与 XXL-JOB 调度框架。 
- **系统设计与架构：**熟悉常用的设计模式，具备系统模块化与高可用架构设计能力。 
- **运维与工具：**熟悉 Linux 操作系统，熟悉Git、Maven、Docker、Jenkins 等开发工具。
- **英语能力：** 通过英语四、六级考试，具备良好的英文阅读与技术文档理解能力，可流畅阅读英文源码与 API 文档，并能进行基础的英语沟通与协作。
