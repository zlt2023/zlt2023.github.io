<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Hello World</title>
    <url>/2026/01/21/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
  </entry>
  <entry>
    <title>流量漏斗模型与高并发架构：从零设计千万级秒杀系统</title>
    <url>/2026/04/02/seckill-system-design/</url>
    <content><![CDATA[<p>最近独立设计并开发了一套高并发秒杀系统。在这个项目中，系统不仅需要应对大流量冲击，还要保证绝对不发生“超卖”问题，同时要兼顾用户体验和系统可用性。今天就带大家从零开始，复盘这套秒杀系统的完整核心技术方案。</p>
<span id="more"></span>

<h2 id="一、技术栈选型"><a href="#一、技术栈选型" class="headerlink" title="一、技术栈选型"></a>一、技术栈选型</h2><p>俗话说“工欲善其事，必先利其器”，在动手前，首先敲定了全栈系统的技术栈栈：</p>
<ul>
<li><strong>前端</strong>：Vue 3 + Vite + Element Plus + Pinia + Axios</li>
<li><strong>后端</strong>：Java 17 + Spring Boot 3 + MyBatis-Plus</li>
<li><strong>缓存</strong>：Redis 7 (Lua 脚本)</li>
<li><strong>数据库</strong>：MySQL 8</li>
<li><strong>消息队列</strong>：RabbitMQ</li>
<li><strong>安全认证</strong>：JWT (无状态Token)</li>
<li><strong>构建部署</strong>：Maven</li>
</ul>
<p>整体架构采用了<strong>前后端分离</strong>模式，用户端与管理端也进行了独立拆分，保证了核心买家链路与后台管理链路相互隔离。</p>
<hr>
<h2 id="二、核心架构思想：流量漏斗模型"><a href="#二、核心架构思想：流量漏斗模型" class="headerlink" title="二、核心架构思想：流量漏斗模型"></a>二、核心架构思想：流量漏斗模型</h2><p>面对瞬时极大的并发流量，如果让所有请求都打到数据库，系统必然瞬间崩溃。秒杀架构的核心思想其实非常简单，就是四个字：<strong>层层拦截</strong>（即流量漏斗模型）。绝不让高并发流量直接打到 MySQL。</p>
<p>以下是我们的 6 层防御体系设计：</p>
<ol>
<li><strong>第1层：前端防护 (拦截大量重复点击)</strong><ul>
<li>按钮防抖：用户点击抢购后，按钮立刻置灰 3 秒。</li>
<li>精确拦截：倒计时未归零时按钮处于禁用状态。</li>
</ul>
</li>
<li><strong>第2层：Nginx 限流 (拦截恶意&#x2F;超频请求)</strong><ul>
<li>通过 <code>limit_req</code> 模块限制单 IP 每秒最大请求次数。</li>
<li>配合 IP 黑名单过滤非法请求，拦截约 40% 的异常流量。</li>
</ul>
</li>
<li><strong>第3层：接口安全校验 (拦截脚本&#x2F;Bot请求)</strong><ul>
<li>JWT 身份认证，保证真实用户才能下单。</li>
<li><strong>动态路径 Token 校验</strong>：秒杀地址不是固定的，而是先获取一个 60 秒过期的 <code>pathToken</code>，再带着它发请求，拦截 30% 脚本直刷请求。</li>
</ul>
</li>
<li><strong>第4层：JVM 内存标记 (最快失败，L1缓存)</strong><ul>
<li>通过 <code>localOverMap</code> 在 JVM 本地缓存商品售罄状态。一旦售罄，直接在 JVM 层驳回请求，连 Redis 都不用去查。</li>
</ul>
</li>
<li><strong>第5层：Redis Lua 原子扣减 (核心准入校验)</strong><ul>
<li>使用 Hash 存储活动信息，Set 判重。</li>
<li>利用 Lua 脚本原子性地完成：状态校验、时间校验、库存预占、防止重复购买。一次网络往返搞定所有验证，将 99% 的无效请求拦截在 Redis 层。</li>
</ul>
</li>
<li><strong>第6层：RabbitMQ 异步入队 (保护数据库)</strong><ul>
<li>只有 Redis 扣减成功（也就是那 1% 能买入预占名额的用户）的请求，才会被扔进消息队列。</li>
<li>消费者以平稳的速度消费订单消息，实现“削峰填谷”，最终将数据落盘 MySQL。</li>
</ul>
</li>
</ol>
<hr>
<h2 id="三、Redis-如何抗住高并发与防超卖？"><a href="#三、Redis-如何抗住高并发与防超卖？" class="headerlink" title="三、Redis 如何抗住高并发与防超卖？"></a>三、Redis 如何抗住高并发与防超卖？</h2><p>如何解决<strong>超卖</strong>问题是秒杀系统的灵魂。在我的方案中，强依赖 Redis Hash 数据结构和 Lua 脚本来实现绝对安全的原子扣减。</p>
<p>我们统一定义了缓存 Key 规范：</p>
<ul>
<li><code>seckill:info:&#123;id&#125;</code>：Hash 结构，存储核心元数据（价格、库存、开始&#x2F;结束时间、上架状态）。</li>
<li><code>seckill:bought:&#123;id&#125;</code>：Set 结构，存储已成功购买的用户 ID，防止一人多买。</li>
</ul>
<p><strong>Lua 脚本的核心拦截逻辑</strong>：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 1. 检查行政状态 (status != 1 表示活动已下架/暂停，直接拒绝)</span></span><br><span class="line"><span class="comment">-- 2. 校验时间视窗 (当前毫秒时间不在 startTime 和 endTime 范围内，直接拒绝)</span></span><br><span class="line"><span class="comment">-- 3. 检查重复购买 (利用 SISMEMBER 判断用户是否在 bought 集合中，是则拒绝)</span></span><br><span class="line"><span class="comment">-- 4. 校验库存并原子扣减</span></span><br><span class="line"><span class="keyword">if</span> (stock &gt; <span class="number">0</span>) <span class="keyword">then</span></span><br><span class="line">    HINCRBY KEYS[<span class="number">1</span>] <span class="string">&quot;stock&quot;</span> <span class="number">-1</span></span><br><span class="line">    SADD KEYS[<span class="number">2</span>] ARGV[<span class="number">1</span>]</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span> <span class="comment">-- 预占成功，进入 MQ 异步下单流</span></span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span> <span class="comment">-- 库存不足，无情驳回</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>这段 Lua 脚本在 Redis 中单线程原子执行，不仅<strong>天然解决了高并发下的超卖与重入互斥</strong>问题，其极致的性能也足以应对洪峰流量。</p>
<p><em>(为了避免秒杀开始瞬间穿透，还设计了缓存预热机制，在 Spring Boot 启动或管理端手动重载时，提前将秒杀商品元数据写入 Redis Hash 并设置过期缓冲时长，防止内存被无效活动占满。)</em></p>
<hr>
<h2 id="四、RabbitMQ-异步下单与可靠性保障"><a href="#四、RabbitMQ-异步下单与可靠性保障" class="headerlink" title="四、RabbitMQ 异步下单与可靠性保障"></a>四、RabbitMQ 异步下单与可靠性保障</h2><p>用户在前端点击秒杀看到“排队中，请等待结果…”时，后端实际上正在通过 RabbitMQ 异步平稳地生成订单。</p>
<p><strong>异步下单流程简述</strong>：</p>
<ol>
<li>Redis Lua 返回预占成功 (<code>1</code>) 后，发一条包含雪花算法 requestId 标识的消息至 <code>seckill.exchange</code>。</li>
<li>消费者监听到消息，通过获取 Redis 分布式锁 (<code>setnx</code>) 避免高并发下的极低概率重复消息执行。</li>
<li>数据库开启事务：执行扣减 MySQL 库存 (<code>UPDATE ... SET available_stock = available_stock - 1 WHERE available_stock &gt; 0</code>)。这里利用乐观锁机制做数据库兜底防超卖。</li>
<li>插入 MySQL 订单数据 <code>t_seckill_order</code>，利用唯一的联合索引进行最终重买兜底。</li>
<li>更新 Redis 中的抢购结果为排队成功（这样前端轮询 <code>/api/seckill/result</code> 时就会看到抢购成功，并调转去支付），最后在 MQ 消费端进行手动 ACK 确认。</li>
</ol>
<p><strong>万一因为某些原因失败了怎么办？</strong><br>如果兜底下单失败（比如 MySQL 并发冲突或宕机问题），程序捕获异常后必须<strong>回滚 Redis 预占</strong>：利用 <code>INCR</code> 恢复 Redis 库存，并用 <code>SREM</code> 移除该用户的购买记录标记，随后更新抢购状态为“抢购失败”。该过程亦包含 3 次失败重投策略，超过上限则交由死信队列（DLQ）进行人工监控并处理。</p>
<hr>
<h2 id="五、超时未支付如何释放库存？（延迟队列机制）"><a href="#五、超时未支付如何释放库存？（延迟队列机制）" class="headerlink" title="五、超时未支付如何释放库存？（延迟队列机制）"></a>五、超时未支付如何释放库存？（延迟队列机制）</h2><p>真正的线上秒杀还要面临一个真实痛点考验：恶意的羊毛党或者纠结的用户锁了库存但不去付款（占坑不拉屎）。</p>
<p>在消费者处理完创建订单并提交事务后，系统会向 RabbitMQ 下发一条带有 <strong>15 分钟 TTL</strong> 限制的延迟消息。</p>
<ol>
<li>15分钟过后，消息过期并被转发至设置好的<strong>死信交换机</strong> (DLX)，流入 <code>cancel.queue</code>。</li>
<li>超时消费者拉取消息，检索该订单状态。</li>
<li>如果此时订单仍是 <code>待支付 (0)</code>，执行原子更新取消：<code>UPDATE status = 3 WHERE status = 0</code>。</li>
<li><strong>反向补偿链路</strong>：MySQL 增加库存归还 <code>+1</code>、Redis 恢复库存归还 <code>+1</code>、移除防重购买 Set 集合内标志，并且<strong>清除 JVM 的已售罄缓存标记</strong>（这就意味着之前卖光的商品，此时可能又重新亮起购买按钮允许后续排队者去抢空出的名额了）。</li>
</ol>
<hr>
<h2 id="六、安全防护设计：不给羊毛党任何机会"><a href="#六、安全防护设计：不给羊毛党任何机会" class="headerlink" title="六、安全防护设计：不给羊毛党任何机会"></a>六、安全防护设计：不给羊毛党任何机会</h2><p>单靠后端性能优化还不够，对各种脚本协议和恶意篡改必须重重防护：</p>
<ol>
<li><p><strong>接口动态隐藏（动态路径设计）</strong><br>这招杀伤力极大。如果前端的秒杀接口是 <code>/api/seckill/buy?goodsId=1</code> 这样固定的，那黄牛可以直接越过前端发送请求。我的方案中，必须先请求后台 <code>GET /api/seckill/path/&#123;id&#125;</code> 分配一个随机 <code>pathToken</code> (存入Redis, 60s有效期)。提交最终下单时 <code>POST /api/seckill/&#123;token&#125;/execute</code>，匹配不上或过期通通不认，从而完全隐藏了底层的核心接口。</p>
</li>
<li><p><strong>限流切面的设计 (@RateLimit 注解)</strong><br>开发了自定义注解 + AOP 拦截体系 + Redis 计数器。业务只需要标记 <code>@RateLimit(maxCount = 5, seconds = 5)</code>，系统就会自动对请求 IP 和用户账号做限流，拦截极高频非人点击。</p>
</li>
<li><p><strong>接口幂等与防并发竞态的严密校验</strong><br>更新和写操作坚决不用 <code>SELECT .. 然后再 UPDATE ..</code> ，完全利用 SQL 提供的原子防竞态能力 (TOCTOU)，在语句后加入状态判定和条件锁定操作。</p>
</li>
</ol>
<hr>
<h2 id="七、极端场景数据一致性对账（补偿任务）"><a href="#七、极端场景数据一致性对账（补偿任务）" class="headerlink" title="七、极端场景数据一致性对账（补偿任务）"></a>七、极端场景数据一致性对账（补偿任务）</h2><p>就算上了全套隔离，如果遇到服务器断电宕机怎么办？我在代码结构中专设了 <code>task</code> 包。<br>后台有一个 <code>Scheduled Task</code> 定时任务，扮演<strong>“包青天”</strong>对账角色：每 5 分钟遍历一次所有存活运行中的在线秒杀活动，比对 Redis Hash 中的剩余库存和 MySQL 中的 <code>available_stock</code>。只要两头对不上号，<strong>强制以 MySQL 落盘数据为准覆盖 Redis</strong>（记录详细对账告警日志）。这样即使缓存崩溃也能迅速自我恢复纠正。</p>
<hr>
<h2 id="写在最后"><a href="#写在最后" class="headerlink" title="写在最后"></a>写在最后</h2><p>这套系统耗时两周完整落地，可以说是麻雀虽小五脏俱全。架构设计虽然经典，但只有亲自动手才体会到了其中坑连坑的奥妙：诸如 Lua 脚本和 Redis 数据结构结合的细节、RabbitMQ 分布式解耦导致的最终一致性要求、网络极度延迟情况的前端防抖轮训等等。</p>
<p>面对性能瓶颈分析问题、解决问题，才是做后端的最大乐趣。大家如果有建议或者疑问，欢迎在评论区探讨交流！</p>
]]></content>
      <categories>
        <category>后端开发</category>
      </categories>
      <tags>
        <tag>高并发</tag>
        <tag>秒杀系统</tag>
        <tag>Redis</tag>
        <tag>RabbitMQ</tag>
        <tag>系统架构</tag>
      </tags>
  </entry>
</search>
